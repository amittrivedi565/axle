# Service configuration
[user-service]
HOST=127.0.0.1
PORT=5000
DEFAULT-EXPOSURE=PUBLIC
DEFAULT-AUTH=true

# Custom configured routes
[user-service.routes]
PATH=/orders/:id
METHODS=POST
EXPOSURE=PRIVATE
AUTH-REQUIRED=true

[user-service.routes]
PATH=/orders/:id
METHODS=GET
EXPOSURE=PRIVATE
AUTH-REQUIRED=true

